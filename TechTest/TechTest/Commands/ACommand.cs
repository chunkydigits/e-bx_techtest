namespace TechTest.Commands
{
    /// <summary>
    /// If we were doing more complex stuff, I would add some code here for an action that would change the DB in some way.
    /// This allows us to separate the Command and Query logic into different classes, projects, solutions etc to adhere to a CQRS way of working
    /// </summary>
    public class ACommand
    {
    }
}
